# /components

Shared React components for NomadReady.

## Planned components (Steps 3–6)

- `DestinationCard.tsx`     — Card shown on the home screen
- `SectionBlock.tsx`        — Collapsible section on the Ready page
- `ChecklistItem.tsx`       — Interactive checklist row
- `PhraseRow.tsx`           — Local phrase + translation row
- `JumpNav.tsx`             — Sticky section navigation
- `PassportSelector.tsx`    — Passport picker (expandable in v2)
