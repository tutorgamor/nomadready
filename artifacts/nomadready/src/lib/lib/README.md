# /lib

Utility functions and shared TypeScript types.

## Planned contents (Steps 2–4)

- `types.ts`        — All shared TypeScript interfaces (Destination, VisaInfo, etc.)
- `getData.ts`      — Helper to load and validate JSON data files
